using ProWrite.Core;

namespace ProWrite.UFE.Entity
{
    public enum TextEffect
    {
        NONE = OutlineShadowType.NONE,
        SHADED = OutlineShadowType.SHADED,
        BUBBLE = OutlineShadowType.BubbleSurround
    }
}
