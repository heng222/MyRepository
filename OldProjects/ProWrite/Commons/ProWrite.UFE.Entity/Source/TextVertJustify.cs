using ProWrite.Core;

namespace ProWrite.UFE.Entity
{
    public enum TextVertJustify
    {
        TOP = Valign.TOP,
        BOTTOM = Valign.BOTTOM,
        CENTER = Valign.MIDDLE,
    }
}
